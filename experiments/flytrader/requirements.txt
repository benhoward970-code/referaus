# Core. The simulation is plain numpy.
numpy>=1.24

# Only needed to build the connectome cache from the published export the
# first time; the cached .npz load path does not use them.
pandas>=2.0
pyarrow>=12.0

# Tests.
pytest>=7.0
